apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello
spec:
  parentRefs:
    - name: hello
  rules:
    - backendRefs:
        - name: hello
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /

# PLACEHOLDER — this file shows the *shape* of a SealedSecret, but its
# ciphertext is fake and will NOT decrypt on your cluster: an encrypted value
# is tied to the public key of one specific Sealed Secrets controller.
# Regenerate it with the `kubeseal` command from README.md (step 2), which
# overwrites this file. The result is safe to commit.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: hello
  namespace: default
spec:
  encryptedData:
    token: AgBvA5c9NOT-A-REAL-CIPHERTEXT-REGENERATE-ME1TZWFsZWRTZWNyZXQ=
  template:
    metadata:
      name: hello
      namespace: default
    type: Opaque

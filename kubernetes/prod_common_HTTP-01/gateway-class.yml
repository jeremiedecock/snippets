# Cluster-wide: binds the class name used by every Gateway to the Envoy
# Gateway controller. Installed once per cluster, survives every namespace.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: envoy
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller

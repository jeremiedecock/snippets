apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - image: docker.io/library/nginx:stable-alpine-slim
      name: my-container
      envFrom:
        - configMapRef:
            name: my-config-map
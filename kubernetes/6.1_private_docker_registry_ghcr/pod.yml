apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - image: ghcr.io/jeremiedecock/hello-fastapi:1.0
      name: my-container
      imagePullPolicy: Always
  imagePullSecrets:
    - name: ghcr-secret

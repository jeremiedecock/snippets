apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: snippet-gatewayapi-demo-app2
spec:
  type: ClusterIP
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
